name: pymc-experimental-test
channels:
- conda-forge
- defaults
dependencies:
- pip
- pytest-cov>=2.5
- pytest>=3.0
- dask
- xhistogram
- pip:
  - pymc>=5.1.2  # CI was failing to resolve
  - blackjax
